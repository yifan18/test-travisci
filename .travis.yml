language: node_js
node_js:
  - 8
install: 
  - npm install
script:
  - npm run build
branches:
  only:
  - master
  - stable

before_install:
  - chmod 600 ~\/.ssh/id_rsa
  - openssl aes-256-cbc -K $encrypted_2749aaa3c5b2_key -iv $encrypted_2749aaa3c5b2_iv -in id_rsa.enc -out ~\/.ssh/id_rsa -d
after_success:
  - scp ./ root@$serverhost:$serverpath